plugins {
    id 'groovy-gradle-plugin'
    id 'maven-publish'
}

group = 'com.meeteric.gradle'

repositories {
    // Use the plugin portal to apply community plugins in convention plugins.
    gradlePluginPortal()
}
